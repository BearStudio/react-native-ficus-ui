---
searchable: true
---

import { CodeEditor } from '@components/code-editor';
import PropsTable from "@components/docs/props-table";


# Flex

As `Box` component but with `flex={1}` property by default, it accepts every props from react native `View` component.

## Import

```js
import { Flex } from "@ficus-ui/native";
```

## Usage

<CodeEditor code={`<Box h={200}>
  <Flex bg="red.500" />
</Box>`} />

## Props

Extends every `Box` props.

### `bg`
<PropsTable
  description="The background color property (`backgroundColor` style prop in StyleSheet)."
  prop={{ type: "string", required: false }}
/>

### `bgImg`
<PropsTable
  description="The background image property. Will use `ImageBackground` component as parent wrapper."
  prop={{ type: "ImageSourcePropType", required: false }}
/>

### `bgMode`
<PropsTable
  description="The background image mode property."
  prop={{ type: "`contain` | `cover` | `stretch`", required: false }}
/>

### `m`
<PropsTable
  description="The margin property (`margin` style prop in StyleSheet)."
  prop={{ type: "number | string", required: false }}
/>

### `mt`
<PropsTable
  description="The margin top property (`marginTop` style prop in StyleSheet)."
  prop={{ type: "number | string", required: false }}
/>

### `mb`
<PropsTable
  description="The margin bottom property (`marginBottom` style prop in StyleSheet)."
  prop={{ type: "number | string", required: false }}
/>

### `mr`
<PropsTable
  description="The margin right property (`marginRight` style prop in StyleSheet)."
  prop={{ type: "number | string", required: false }}
/>

### `ml`
<PropsTable
  description="The margin left property (`marginLeft` style prop in StyleSheet)."
  prop={{ type: "number | string", required: false }}
/>

### `mx`
<PropsTable
  description="The margin horizontal property (`marginHorizontal` style prop in StyleSheet)."
  prop={{ type: "number | string", required: false }}
/>

### `my`
<PropsTable
  description="The margin vertical property (`marginVertical` style prop in StyleSheet)."
  prop={{ type: "number | string", required: false }}
/>

### `p`
<PropsTable
  description="The padding property (`padding` style prop in StyleSheet)."
  prop={{ type: "number | string", required: false }}
/>

### `pt`
<PropsTable
  description="The padding top property (`paddingTop` style prop in StyleSheet)."
  prop={{ type: "number | string", required: false }}
/>

### `pb`
<PropsTable
  description="The padding bottom property (`paddingBottom` style prop in StyleSheet)."
  prop={{ type: "number | string", required: false }}
/>

### `pr`
<PropsTable
  description="The padding right property (`paddingRight` style prop in StyleSheet)."
  prop={{ type: "number | string", required: false }}
/>

### `pl`
<PropsTable
  description="The padding left property (`paddingLeft` style prop in StyleSheet)."
  prop={{ type: "number | string", required: false }}
/>

### `px`
<PropsTable
  description="The padding horizontal property (`paddingHorizontal` style prop in StyleSheet)."
  prop={{ type: "number | string", required: false }}
/>

### `py`
<PropsTable
  description="The padding vertical property (`paddingVertical` style prop in StyleSheet)."
  prop={{ type: "number | string", required: false }}
/>

### `h`
<PropsTable
  description="The height property (`height` style prop in StyleSheet)."
  prop={{ type: "number | string", required: false }}
/>

### `w`
<PropsTable
  description="The width property (`width` style prop in StyleSheet)."
  prop={{ type: "number | string", required: false }}
/>

### `minH`
<PropsTable
  description="The minimum height property (`minHeight` style prop in StyleSheet)."
  prop={{ type: "number | string", required: false }}
/>

### `minW`
<PropsTable
  description="The minimum width property (`minWidth` style prop in StyleSheet)."
  prop={{ type: "number | string", required: false }}
/>

### `flex`
<PropsTable
  description="The flex property for container (`flex` style prop in StyleSheet)."
  prop={{ type: "number", required: false }}
/>

### `flexDirection`
<PropsTable
  description="The flex direction property for container (`flexDirection` style prop in StyleSheet)."
  prop={{ type: "`row` | `column` | `row-reverse` | `column-reverse`", required: false }}
/>

### `justifyContent`
<PropsTable
  description="The justify content property for container (`justifyContent` style prop in StyleSheet)."
  prop={{ type: "`flex-start` | `flex-end` | `center` | `space-between` | `space-around` | `space-evenly`", required: false }}
/>

### `alignItems`
<PropsTable
  description="The align items property for container (`alignItems` style prop in StyleSheet)."
  prop={{ type: "`flex-start` | `flex-end` | `center` | `stretch` | `baseline`", required: false }}
/>

### `alignSelf`
<PropsTable
  description="The align self property for container (`alignSelf` style prop in StyleSheet)."
  prop={{ type: "`auto` | `flex-start` | `flex-end` | `center` | `stretch` | `baseline`", required: false }}
/>

### `position`
<PropsTable
  description="The position property for container (`position` style prop in StyleSheet)."
  prop={{ type: "`absolute` | `relative`", required: false }}
/>

### `top`
<PropsTable
  description="The top position property for container (`top` style prop in StyleSheet)."
  prop={{ type: "number", required: false }}
/>

### `borderRadius`
<PropsTable
  description="The border radius property (`borderRadius` style prop in StyleSheet)."
  prop={{ type: "number | string", required: false, defaultValue: "`none`" }}
/>

### `borderColor`
<PropsTable
  description="The border color property (`borderColor` style prop in StyleSheet)."
  prop={{ type: "string", required: false }}
/>

### `shadow`
<PropsTable
  description="The shadow property (`shadowOffset` and `elevation` style props in StyleSheet)."
  prop={{ type: "number", required: false }}
/>

### `opacity`
<PropsTable
  description="The opacity property (`opacity` style prop in StyleSheet)."
  prop={{ type: "number", required: false }}
/>
